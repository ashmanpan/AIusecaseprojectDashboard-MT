version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static HTML/JS site - no build needed"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
    excludePatterns:
      - 'amplify/**/*'
      - 'cloudformation/**/*'
      - 'node_modules/**/*'
      - '.git/**/*'
